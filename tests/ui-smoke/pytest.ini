[pytest]
# UI-Smoke tests are NOT collected by default — they require a running
# clean-boot stack and are expected to fail on G1 (no Create Campaign button).
# Run manually:  pytest tests/ui-smoke/ -v
# Or via script: scripts/ui-smoke-audit.sh

markers =
    ui_smoke: UI-smoke test requiring a running clean-boot stack (not CI)
