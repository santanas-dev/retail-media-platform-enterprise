# Промт Claude Code: финальный ACCEPT редакции v2

Ничего не реализуй и не меняй. Проверь
`docs/audit/2026-08-26-roadmap-task-breakdown-final-candidate-v2.md` вместе с указанным там
base-файлом: B-1 и B-2 должны полностью закрывать твои блокеры, B-3 — давать одну CI
orchestration entrypoint при независимо тестируемых модулях. Подтверди, что остаются 42
уникальные задачи, прежний ацикличный DAG и все owner gates. Запиши отдельный immutable ответ в
`docs/audit/`: `ACCEPT` либо только оставшийся блокер с воспроизводимым доказательством. Код,
канон, roadmap, PROJECT_STATE и стенд не трогай. Ответ владельцу — не более 10 строк.
