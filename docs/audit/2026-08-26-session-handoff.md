# Передача состояния: аудиты → roadmap governance → RM-GOV-001/002

> ## ⚠️ НЕ КАНОН — рабочая передача состояния
>
> | | |
> |---|---|
> | **Тип** | Handoff (состояние на конец сессии) |
> | **База** | `develop @ 2b935bb` = `origin/develop @ 2b935bb`; стенд `stand-27dc397`, schema `036` |
> | **Дата** | 2026-08-26 |
> | **Автор** | Claude Code |
> | **Состояние очереди** | `RM-GOV-001` — **ACCEPT получен**; `RM-GOV-002` — выполнен, **ждёт ACCEPT** |
> | **Коммитов/push** | **нет.** Все файлы untracked |
> | **Отменён** | — |

## 1. Что сделано за сессию

### 1.1 Аудиты

| Запись | Итог |
|---|---|
| `2026-08-26-code-and-security.md` | P0-утечка `AUTHZ-CROSS-PORTAL-001` закрыта — проверено моим tamper-циклом; 4 открытые находки |
| `2026-08-26-canon-and-architecture.md` | 3 🔴 (порядок истины, два канона roadmap, HMAC вместо Ed25519), 5 разрывов ТЗ |
| `2026-08-26-work-plan.md` | WP-1…WP-12, блок W0+ |
| `2026-08-26-doc-governance-proposal.md` | правила `docs/audit/`, механизм видимости через индекс `AGENTS.md` |
| `2026-08-26-codex-independent-audit.md` | Codex: 8 находок, **три я пропустил** — C1 (смоуки нарушают Done Gate), C2 (13 UI без journey), C6 (опасный roadmap-mutator) |
| `2026-08-26-claude-response-to-codex.md` | принял 4 корректировки к себе, 3 возражения, 1 новая улика |
| `2026-08-26-operator-scope-experiment.md` | **доказал прогоном:** `operator` и `analyst` видят **0 строк** на всех 36 FORCE-RLS таблицах; инверсия модели — сотрудник ритейлера видит инвентарь только через scope рекламодателя |
| `2026-08-26-claude-reconciliation.md` | R1–R8 подтверждены, возражений нет |
| `2026-08-26-claude-task-breakdown-reconciliation.md` | 40 задач, DAG чист, 11 находок не названы, 10 поправок |
| `2026-08-26-claude-final-feasibility-reconciliation.md` | 3 блокера B-1…B-3 |
| `2026-08-26-claude-final-acceptance-v2.md` | **ACCEPT** редакции v2 (42 задачи) |

### 1.2 Владелец утвердил

Очередь из 42 задач, порядок `G → E0 → S → U → branches`, восемь решений (§1 v2), плюс:
`external-plan` — штатный `kind`; схема остаётся в `docs/product/`.

### 1.3 `RM-GOV-001` — ACCEPT получен

- `docs/product/roadmap.schema.json` — JSON Schema; `additionalProperties: false` везде,
  поэтому статус функции, шаг journey или процент в SSOT не протащить.
- `scripts/ci/check-roadmap-schema.py` — валидатор; **модуль**, а не CI-gate (правило B-3:
  единственная entrypoint у `RM-GOV-004`).
- `scripts/ci/fixtures/roadmap.schema.example.yaml` — фикстура самопроверки.
- `docs/architecture/rm-gov-001-roadmap-yaml-design-gate.md` — mini-design.
- По требованию владельца добавлено правило **обязательного конкретного `ref`** для
  machine-verifiable приёмок (схема `if/then` + семантический отсев заглушек `VAGUE-REF`).
  Правило сразу поймало два пробела в моей же фикстуре.

### 1.4 `RM-GOV-002` — выполнен, ждёт ACCEPT

- `docs/product/roadmap.yaml` — **начальный SSOT, 42 задачи**, валиден.
- `docs/product/roadmap-migration-manifest.yaml` — диспозиция **164 строк**:
  `history` 145 · `migrate` 12 · `drop-with-reason` 5 · `deduplicate` 2.
- `scripts/ci/roadmap-migration-counts.py` — именованный read-only скрипт счётчиков.
- `scripts/dev/build-initial-roadmap.py` + `roadmap_acceptance_map.py` — одноразовая миграция.
- **Поправка схемы:** секция `blocked_features` (только `unblocked_by` + `conditions`; `gap`
  остаётся в реестре) + правила `MISSING-UNBLOCK` / `UNBLOCK-DANGLING`. Tamper-матрица — **13
  кейсов, все пойманы**.

## 2. Проверяемое состояние

```bash
python3 scripts/ci/check-roadmap-schema.py --file docs/product/roadmap.yaml  # PASS
python3 scripts/ci/check-roadmap-schema.py --self-test                       # PASS, 13/13 tamper
python3 scripts/ci/roadmap-migration-counts.py                               # PASS 93/13/58
python3 scripts/ci/check-import-boundaries.py                                # clean
python3 scripts/roadmap-consistency-check.py --strict                        # exit 0
```

Ничего не коммитилось. `git status` — только untracked в
`docs/audit/`, `docs/architecture/`, `docs/product/`, `scripts/ci/`, `scripts/dev/`.
Канон (`AGENTS.md`, `CLAUDE.md`, `PROJECT_STATE.md`, ADR, registry) и стенд **не изменялись**.

## 3. Что дальше

### 3.1 Ждёт решения владельца

1. **`ACCEPT` по `RM-GOV-002`** — блокирует старт `RM-GOV-003`.
2. **19 открытых пунктов вне очереди** — legacy-строки с открытым статусом, которые не
   покрывает ни одна из 42 задач: SIEM/Wazuh, PII minimisation, VPN-only доступ, feature flags,
   staged rollout, data governance/retention, нагрузочные профили 40K, часовые пояса. Помечены
   `history` с явным «ОТКРЫТЫЙ — не закрыт». Решение: отдельная задача или реестр открытых решений.
3. Оформлять ли untracked-файлы коммитом (push — отдельное разрешение).

### 3.2 Следующая задача очереди

**`RM-GOV-003`** — односторонний генератор `roadmap.yaml + registry + evidence → roadmap.md +
XLSX + метрики`. Приёмка: детерминированный вывод, повторный прогон даёт чистый diff, обе XLSX
sheets и колонки проверены, ноль ручных derived-статусов.

Затем `RM-GOV-004` (структурный guard, единственная CI entrypoint) → `RM-GOV-006` (правило факта
и требования) → `RM-GOV-005` (canonical cutover, owner approval) → **Gate G** → `RM-ENV-001`.

### 3.3 Правила, действующие в новом контексте

- Одна implementation-задача за раз; следующая — после evidence, ревью Codex и синхронизации.
- Claude Code реализует, Codex независимо проверяет, владелец утверждает архитектуру,
  protected boundaries и внешние действия.
- Автор артефакта не подписывает его аудит.
- Ручные правки generated `roadmap.md`/XLSX запрещены после cutover.
- Полный UI-smoke против общего стенда запрещён (долг `UI-SMOKE-VS-STAND`).
- Merge, release, deploy, применение миграции, изменение канона — поштучное разрешение владельца.

## 4. Открытые находки, ещё не заведённые в очередь

Из моих аудитов не названы ни в одной приёмке: A3 (`requirements/README.md` устарел),
A4 (ADR-018/019 вне индекса архитектуры), A5 (формат заголовка ADR-018), A6 (вывод Hermes не
приземлён в §21 ТЗ, остались `.hermes/tmp-*.py`), D2 (`PROJECT_STATE.md` 268 КБ читается перед
каждой задачей), D3 (34 из 47 архитектурных документов историчны), N2 (`AGENTS.md:51` описывает
XLSX как «4 колонки», фактически 11). Большинство логично сложить в приёмку `RM-GOV-005`.

## RM-GOV-003 — выполнено, ожидает ACCEPT

`ACCEPT` по `RM-GOV-002` получен от владельца 2026-08-26; задача закрыта в SSOT с тремя
verified evidence_refs. `RM-GOV-003` выполнен следом.

| Артефакт | Назначение |
|---|---|
| `scripts/ci/roadmap-generate.py` | односторонний генератор; модуль, а не блокирующий CI-job (правило B-3) |
| `docs/product/generated/roadmap.generated.md` | Markdown-проекция |
| `docs/product/generated/roadmap.generated.xlsx` | две вкладки: 42 задачи и 58 функций |
| `docs/product/generated/roadmap-metrics.generated.json` | машиночитаемые метрики |
| `docs/product/generated/README.md` | правила каталога: только чтение, не канон |
| `docs/architecture/rm-gov-003-roadmap-generator-design-gate.md` | mini-design, отклонения, последствия |

Приёмка доказана прогонами: `--check-clean-diff` → `CLEAN`; `--self-test` → 13/13;
детерминизм подтверждён между процессами при разных `PYTHONHASHSEED`, `TZ` и `LC_ALL`
(sha256 всех трёх артефактов совпал); обе вкладки открыты и сверены с входами
(42/42 и 58/58, ID совпадают полностью); формул нет ни в сгенерированной книге, ни в
каноническом `roadmap-s020-2026-07-10.xlsx` — проверено на уровне XML.

Метрика `закреплено в CI-subset = 43` совпала с числом в каноническом `roadmap.md`,
посчитанная независимо — это внешняя сверка, а не совпадение по построению.

### Что задача открыла для следующих

1. **`roadmap-consistency-check.py` станет тавтологией** после cutover: он сравнивает registry
   с вручную ведомой книгой. Заменяет его `RM-GOV-004`, а не переключение на новый файл.
2. **165 из 322 строк `roadmap.md` не воспроизводимы генератором** (UX backlog 121, честные
   проценты 16, технические направления 15, бизнес-карта по ролям 13). Манифест `RM-GOV-002`
   дал диспозицию только строкам XLSX. Нужно решение владельца — вход в `RM-GOV-005`.
3. **Дефект в каноне:** `roadmap.md` заявляет `backend/service без UI-journey: 10` в разделе
   «посчитано программно», а собственный заголовок того же файла и registry дают `13`.
4. **`scripts/tamper-test-roadmap-guard.py` красный на чистом `HEAD 2b935bb`** (3 passed,
   1 failed) — воспроизведено на `git archive HEAD`, дефект не мой. Фикстура устарела:
   строка «Согласование кампаний» больше не overclaim, потому что `campaign.approve/reject`
   стали reachable. Направление OVERCLAIM с тех пор без живого доказательства. → `RM-GOV-004`.

Канон не изменён: `git status --porcelain` по `roadmap.md`, `roadmap-s020-2026-07-10.xlsx`,
`feature-registry.yaml`, `PROJECT_STATE.md`, `AGENTS.md`, `user-journeys.md` — пусто.

## RM-GOV-004 — выполнено, ожидает ACCEPT

`ACCEPT` по `RM-GOV-003` получен 2026-08-26. `RM-GOV-004` выполнен.

| Артефакт | Назначение |
|---|---|
| `scripts/ci/roadmap-governance-guard.py` | единственный CI-entrypoint governance; модули `schema`, `drift`, `metrics`, `ssot` |
| `.github/workflows/phase1-ci.yml` | **изменён:** новый job `roadmap-governance-guard`; `release-gate` вырос с 14 до 15 job |
| `scripts/tamper-test-roadmap-guard.py` | **изменён:** перенацелена устаревшая OVERCLAIM-фикстура (находка §6.4 из RM-GOV-003) |
| `docs/architecture/rm-gov-004-roadmap-governance-guard-design-gate.md` | mini-design |

Это первая задача очереди, которая трогает **отслеживаемые** файлы. До неё все изменения
были untracked.

Приёмка: гейт `PASS`, tamper-матрица **15/15** по пяти требуемым измерениям
(`schema`, `dependencies`, `drift`, `metrics`, `SSOT`) плюс зелёный baseline.
Legacy-тест `tamper-test-roadmap-guard.py`: было `3 passed, 1 failed`, стало `4 passed, 0 failed`.

### Не доказано

**Приёмочный `ci_job` не подтверждён.** Job заведён и локально исполняется теми же командами,
но ни разу не запускался в GitHub Actions — в репозитории ничего не закоммичено.
Evidence записан как `command`, не `ci_run`. Пока владелец не решит вопрос коммита и push,
`RM-GOV-004` доказан поведением, но не прогоном CI.

### Что задача добавила к правилам

Каждый tamper-кейс перед запуском гейта проверяется на **инертность**: отпечаток дерева
обязан измениться, а смысловой предикат — подтвердить, что подмена вступила в силу.
Повод — собственная ошибка: первая фикстура «цикл в графе» дописывала второй ключ
`dependencies` в блок, где уже был `dependencies: []`; YAML взял последний, байты
изменились, смысл нет, гейт остался зелёным. Инертная фикстура теперь падает с диагнозом
«фикстура инертна», а не выглядит как сломанный гейт. Механизм проверен обратным прогоном.

### Осознанно не сделано

`roadmap-consistency-audit` не трогался: он интерпретирует другое правило и станет
тавтологией только после cutover — это вход в `RM-GOV-005`.
Три legacy-мутатора канонического XLSX (`generate_roadmap.py`, `update_roadmap_v26.py`,
`fix_roadmap_qa.py`) объявлены в `DECLARED_MUTATORS` с пометкой «карантин за `RM-GOV-005`»;
множество мутаторов закрыто, чтобы новый не появился незамеченным, но удаление старых —
работа cutover'а.

Следующая задача очереди: `RM-GOV-006` (единое правило факта и требования, ADR-020),
затем `RM-GOV-005` (canonical cutover) и Gate G. Стартовать только после `ACCEPT` по 004.

### Поправка по решению владельца 2026-08-26

`RM-GOV-004` оставлен в `delivery_status: verification`, а не `done`: приёмка заявляет
`verified_by: ci_job`, а job ни разу не запускался. Локальные проверки сохранены как
evidence. Перевод в `done` — только после commit/push и успешного прогона GitHub Actions.
Значение `verification` уже было в enum схемы `RM-GOV-001` — правка схемы не потребовалась.

Правило владельца закреплено машинно: новое правило `EVIDENCE-KIND` в модуле `schema`
требует, чтобы `done` имел verified evidence того рода, который назвала приёмка
(`ci_job → ci_run`). Проверено: `verification` зелено; `done` с локальным `command` красно;
`done` + verified `ci_run` зелено; `ci_run` со статусом `disputed` красно.
Три уже закрытые задачи остаются зелёными. Tamper-матрица схемы выросла до 15 кейсов.

Разрезы проекции: `done=3, verification=1, planned=38`.

## RM-GOV-006 — выполнено, ожидает ACCEPT

| Артефакт | Назначение |
|---|---|
| `docs/architecture/adr/ADR-020-fact-vs-requirement.md` | правило OD-001: код/тесты = факт, ТЗ/ADR = требование, расхождение = дефект до явного ADR |
| `scripts/ci/roadmap-governance-guard.py` | новый модуль `doc` (владелец RM-GOV-006), 8 проверок |
| `docs/architecture/rm-gov-006-fact-vs-requirement-design-gate.md` | mini-design |

Tamper-матрица гейта выросла с 15 до **20/20**; измерение `doc` — пять кейсов.
Совпадение заявленного порядка истины в `AGENTS.md` и `CLAUDE.md` проверяется машинно
(обе цепочки извлекаются и сравниваются, не декларируются).

### Индексирование не выполнено — намеренно

Внести ADR-020 в индекс Sources of Truth значит отредактировать `AGENTS.md`, а Gate G
запрещает правку канона до cutover. Приёмка прочитана так: правило записано, конфликта
с индексом/ADR-процессом не создаёт, внесение в индекс переходит в `RM-GOV-005` под
owner gate `canon_change`. До этого ADR-020 по собственному правилу `AGENTS.md`
формально не канон — состояние объявлено и печатается гейтом.

**Если владелец читает приёмку иначе — как обязанность внести в индекс сейчас — задачу
нужно вернуть в `verification` до Gate G.**

### Три предсуществующих конфликта самого индекса — вход в RM-GOV-005

1. `docs/architecture/README.md` объявляет `ADR-001..ADR-015`, `AGENTS.md` — `ADR-001..ADR-019`.
2. ADR-018 и ADR-019 есть на диске, но отсутствуют в Active-таблице README; по правилу
   индекса они формально не канон — при том что **ADR-018 задаёт модель арендатора,
   на которую опирается `RM-STAB-003`**.
3. 34 документа верхнего уровня несут баннер `SUPERSEDED`; таблица README перечисляет 10.
   Расходятся 24.

Все три печатаются гейтом на каждом прогоне как объявленные расхождения: baseline
зелёный, дефекты названы поимённо, не спрятаны.

Разрезы проекции: `done=4, verification=1, planned=37`.
Следующая задача очереди: `RM-GOV-005` — canonical cutover, owner gate `canon_change`.

## RM-GOV-005 — canonical cutover выполнен, статус verification

`ACCEPT` по `RM-GOV-006` получен. Cutover выполнен по трём решениям владельца:
проза вынесена в отдельные документы; legacy-мутаторы в `scripts/legacy/` с баннером;
job `roadmap-consistency-audit` удалён, функцию берёт `roadmap-governance-guard`.

Статус `verification`, а не `done`: приёмка заявлена `verified_by: owner`, задача несёт
`owner_gate: canon_change` — закрывается утверждением владельца на **Gate G**.

### Что изменилось

| Было | Стало |
|---|---|
| `docs/product/roadmap.md` (канон де-факто, вне индекса) | архив `docs/product/history/roadmap-2026-08-26.md` + 4 тематических документа + генерируемая проекция |
| `docs/product/roadmap-s020-2026-07-10.xlsx` (Tier 2 индекса) | архив `docs/product/history/`; действующая книга генерируется |
| три мутатора книги в `scripts/` | `scripts/legacy/` с баннером `QUARANTINED` |
| job `roadmap-consistency-audit` (blocking) | удалён; живые направления → модуль `registry` гейта |
| `release-gate` 15 job | снова 14 |
| README `ADR-001..ADR-015` против AGENTS.md `ADR-001..ADR-019` | оба `ADR-001..ADR-020` |
| ADR-018/019 вне Active-таблицы | внесены вместе с ADR-020 |
| 34 баннера `SUPERSEDED` против 10 строк таблицы | расхождений нет |
| `Next: PORTAL-UX-POLISH-001A3` | `Next: Gate G`, затем `RM-ENV-001` |

165 строк прозы перенесены ровно — то же число, что `RM-GOV-003` измерил как
невоспроизводимое. Гейт: шесть модулей чисты, tamper-матрица **21/21** по семи измерениям.

### Удалённый job заменён, а не отброшен

`roadmap-consistency-check.py` имел четыре направления; тавтологией стало **одно**
(registry ↔ рукописная книга). Три живых — валидация registry с требованием реального
smoke, orphan smoke, членство в `ci-subset` — перенесены в модуль `registry`.
Тавтологическое направление и его исключительные помощники удалены из скрипта.

### Поправка к моей находке из RM-GOV-003

Я записал «канон заявляет `service без UI-journey: 10` при 13 в registry» как ложное
число. **Неточно.** 13 — все service-функции, 10 — только `reachable`
(заблокированы `playlist.build`, `license.view`, `license.upload`). Оба числа верны при
разных определениях; дефект — неоднозначная подпись, а не неверный счёт. Строка ушла
вместе с документом.

### Открыто

- **Gate G** — утверждение cutover владельцем.
- **`RM-GOV-004`** в `verification`: **ни один CI-прогон не состоялся, ничего не
  закоммичено.** «CI green» доказан локально теми же командами, не прогоном Actions.
- **19 открытых legacy-пунктов** вне очереди — решение не принято.

## Ответ на вердикт владельца по Gate G (2026-08-26)

Gate G не принят. Три замечания; два закрыты, третье упирается в решение о push.

### 1. Ложное срабатывание детектора SUPERSEDED — исправлено

Владелец прав: гейт печатал `SUPERSEDED-UNLISTED (сейчас 1)`, потому что искал **слово**
`SUPERSEDED` в первых 2000 символах и считал вытесненным документ, который про баннер лишь
рассказывает — в том числе собственный design gate `RM-GOV-005`.

Детектор переписан на конвенцию `AGENTS.md`: документ вытеснен, только если начинается с
HTML-комментария, внутри которого строка начинается с `SUPERSEDED`. Настоящий формат —
`<!--` переводом строки и затем `SUPERSEDED:`; проверка на `<!-- SUPERSEDED` в одну строку
не сработала бы ни на одном файле, это тоже проверено. Итог: 34 файла с баннером,
34 строки в таблице, расхождений в обе стороны нет.

### 2. Устаревший текст расхождения — устранён вместе с классом

Все четыре объявленных расхождения были закрыты этим cutover'ом, поэтому таблица
`DECLARED_DOC_DIVERGENCES` удалена целиком. Вместо неё — настоящие проверки с
**вычисляемыми** числами: `ADR-RANGE`, `ADR-UNINDEXED`, `SUPERSEDED-UNLISTED` и новая
`SUPERSEDED-NO-BANNER`. Устаревать нечему: захардкоженного текста больше нет.
Вывод гейта теперь не содержит ни одной строки «объявленное расхождение».

**Корень дефекта:** пока эти проверки были объявленными расхождениями, ни одна не имела
tamper-кейса — их вывод считался ожидаемым, поэтому фикстуры для них никто не писал.
Добавлены четыре кейса, включая **зелёный** «документ лишь упоминает слово SUPERSEDED»,
который охраняет отсутствие именно найденного ложного срабатывания.
Матрица: 21 → **25/25**.

### 3. Триаж 19 открытых пунктов — выполнен

19 строк = 13 тем (шесть задвоены между листами legacy-книги).

- **Две закрыты доказательствами:** Admin Web Auth Shell (`self.login` reachable, smoke в
  `ci-subset`); Channel Orchestrator — отложен **принятым ADR-019** с 2026-07-20, то есть
  никогда и не был открытым вопросом, книга просто не знала про ADR.
- **Одиннадцать сведены в семь решений** `OD-009…OD-015`, записаны в SSOT со статусом
  `open` и попадают в проекцию. Ни одно не решено мной: каждое меняет объём или требование.
- Разбор: `docs/audit/2026-08-26-open-legacy-items-triage.md`.

Триаж уточнил два факта, которые legacy-книга смазывала: сброс пароля и приглашения
рекламодателя **reachable** — отсутствует только self-service путь; `device.health_view`
**reachable** — открыт вопрос объёма, а не наличия.

### 4. CI green — не закрыто, требует решения владельца

Ни одного прогона GitHub Actions не было: в репозитории ничего не закоммичено, `develop`
локально равен `origin/develop` (`2b935bb`). `git commit` и `git push` не входят в allow-лист
проекта, а `CLAUDE.md` требует явного разрешения владельца на push для конкретной задачи.

Состав будущего коммита: 9 изменённых отслеживаемых файлов, 7 переименований
(6 в карантин, 2 в архив), 24 новых пути.

## Первый прогон CI — красный, два дефекта исправлены

`35c6b29` запушен в `develop`. Прогон `32999576636`: job `Roadmap Governance Guard`
дал `FAIL — 2 нарушений`. Оба дефекта мои; ни один не совпал с тем, что я предсказал
владельцу заранее (ожидал расхождение версий `openpyxl` — в CI встала та же 3.1.5).

**1. `No module named 'jsonschema'`.** Я убрал зависимость из установки, проверив импорты
грепом по верхнему уровню; `jsonschema` импортируется внутри функции. Возвращена; состав
пакетов теперь получен обходом AST по всем загружаемым модулям — `jsonschema`, `openpyxl`,
`pyyaml`. Гейт при этом сработал правильно: `MODULE-ERROR` и красный, а не молчаливый пропуск.

**2. `DRIFT` на книге.** Содержимое совпадало, байты — нет: `ZIP_DEFLATED` даёт разный
вывод при разной сборке zlib. Моё «байт-в-байт между процессами» было верно только внутри
одного окружения, а проверялось именно так. Книга теперь пишется `ZIP_STORED` (138 КБ
вместо 20 КБ), zlib выходит из уравнения. Добавлен диагноз: при расхождении генератор
называет, что именно разошлось — состав записей, конкретные записи или только упаковка.

Self-test генератора 13 → 14/14. Остальные 13 job прогона были зелёными.

## Второй прогон — тоже красный; третья причина

`33000138974` на `30bd3e9`: `jsonschema` починен, модуль `schema` чист, но `DRIFT` остался.
Новый диагноз сразу показал, что различаются **сами записи**, а не упаковка.

Причина: openpyxl сериализует XML через `lxml`, если он установлен, и через stdlib иначе.
Локально `lxml 6.1.1`, на раннере его нет. Доказано, а не предположено: локальная генерация
с `OPENPYXL_LXML=False` дала `034035570532` — ровно тот хеш, который CI назвал сгенерированным.

Исправлено: `OPENPYXL_LXML=False` на уровне модуля, до любого импорта openpyxl в процессе;
`render_xlsx` проверяет, что переключение подействовало, и падает с объяснением, если нет.
Self-test генератора 13 → 15/15: добавлены «OPENPYXL_LXML=True не меняет байты» и
«книга хранится без сжатия».

**Урок.** Три красных подряд из одного корня: я проверял «детерминировано» двумя прогонами
на одной машине. Утверждение держалось ровно до чужой. Правильная проверка — воспроизвести
условие цели: прогнать гейт с заблокированным `lxml`, как на раннере. Сделано, зелено.
Надо было до первого push.

## Третий прогон зелёный — RM-GOV-004 закрыт

Прогон **33000750265** на `47be5dd`: `completed / success`, 41 job, **0 провалов**,
`release-gate success`. Job `Roadmap Governance Guard` — `success`: шесть модулей чисты,
tamper-матрица **25/25 на раннере**.

`RM-GOV-004` переведён в `done`. Evidence: два `command`, **`ci_run`** (`gh run 33000750265`)
и `artifact`. Проверено, что правило `EVIDENCE-KIND` пропустило именно из-за `ci_run`:
удаление этой записи при сохранении локальных `command` делает валидатор красным.

Условие приёмки `RM-GOV-005` «CI green» тем же прогоном **выполнено**. Задача остаётся в
`verification` — открыт только owner approval на Gate G.

Разрезы: `done=5, verification=1, planned=36`; задач с verified evidence — 6.

### История трёх прогонов

| Прогон | SHA | Итог | Причина |
|---|---|---|---|
| 32999576636 | `35c6b29` | failure | `jsonschema` импортируется внутри функции — греп по шапке его не увидел; плюс `DRIFT` |
| 33000138974 | `30bd3e9` | failure | `DRIFT` остался: openpyxl сериализует XML через `lxml`, если тот есть; локально есть, на раннере нет |
| 33000750265 | `47be5dd` | **success** | бэкенд XML закреплён на stdlib, книга хранится без сжатия, оба свойства покрыты self-test |

Общий корень обоих красных: «детерминировано» проверялось двумя прогонами на одной машине.
Проверка условия цели — прогон с заблокированным `lxml` — теперь есть и зелёная.

## Gate G принят · этап G закрыт · RM-ENV-001 выполнен

### Закрытие этапа G

`RM-GOV-005` → `done`. Gate G записан в SSOT: `approved_on: 2026-08-26`.

При этом обнаружился настоящий пробел: `RM-GOV-001` стоял `done` при `owner_gate`, а места
для факта вашего разрешения в схеме не было. Схема дополнена (`gates.approved_on`,
`approval_note`), добавлены два правила — `OWNER-GATE-UNGRANTED` (задача с невыданным
owner gate не может быть `done`) и `GATE-NOT-APPROVED`. Правило сразу нашло `RM-GOV-001`;
разрешение проставлено. Tamper-матрица схемы 15 → 16.

### RM-ENV-001 — инвентарь окружений

`docs/product/environment-inventory.yaml`, пять окружений, каждое поле из собственного прогона.

| Окружение | Достижимость | Идентичность | Доказательство |
|---|---|---|---|
| `192.168.110.81` стенд | 200 | `stand-27dc397`, schema `036` | **да** |
| `192.168.110.77` preview | **000** на трёх портах | не наблюдалась | нет |
| santa2 DEV `:3100` | 200 | отсутствует | нет |
| santa2 PROD `:3000` | 200 | отсутствует | нет |
| GitHub Actions | — | SHA даёт GitHub | да, только о поведении на SHA |

**Почему DEV/PROD не доказательство — три проверенные причины, а не декларация:**
оба портала обслуживает ОДИН control-api на `:8000`; этот API отдаёт
`git_sha=unknown, schema_head=null`; и работает он из **изменяемого рабочего клона**
`/home/santa2/work/retail-media-platform-enterprise`, запущен 2026-08-25 14:26:31 без
`--reload` — исполняется код дерева на момент старта, клон с тех пор ушёл вперёд.
Дополнительно: PROD-портал поднят Vite dev-сервером, то есть это не production-сборка.

Очищены три документа, подававшие `.77` как рабочий адрес: `docs/runbook/local-preview.md`,
`docs/product/production-gaps-triage.md`, `docs/architecture/release-versioning.md`.
Записи `docs/audit/` намеренно не тронуты — они датированы и не редактируются.

**Модуль `env`** в гейте: восемь проверок, сверка стенда с `base.stand_baseline` на каждом
прогоне. Живость намеренно НЕ проверяется — у CI нет маршрута в эту LAN, и молча зеленеющая
проверка живости была бы ложью. Матрица гейта 25 → **31/31** по девяти измерениям.

**Повторился класс дефекта, найденный владельцем.** Первая реализация правила про `.77`
сканировала прозу и покраснела на собственном пояснении инвентаря, где запрещённые слова
перечислены как запрещённые. Починено в корне: правило читает структурное поле
`disposition`, свободный текст на вердикт не влияет. Добавлен зелёный охранный кейс.
Отдельно детектор инертности фикстур поймал ещё один дубликат ключа YAML — фикстуры
измерения `env` переведены на структурную правку.

Статус `verification`, не `done`: `owner_gate: scope_decision` не выдан. Приёмка прямо
оставляет решение «retire или upgrade `.77`» владельцу.

### Дубликаты ключей YAML — класс дефекта закрыт машинно

Прогон `33003166965` на `9b88ae8` — success, 0 провалов; модуль `env` зелёный на чистом
раннере, то есть не зависит от моей машины. `ci_run` записан в evidence `RM-ENV-001`.

При записи этого evidence я допустил **третий за сессию** дубликат ключа YAML: у записи
`kind: command` оказалось два `ref`, YAML молча взял последний, и команда подменилась
номером прогона. Валидатор при этом сказал `PASS` — `yaml.safe_load` схлопывает дубликат
ещё до проверок.

Раньше этот капкан ловил только детектор инертности фикстур, и только внутри tamper-кейсов.
Теперь закрыт в корне: валидатор читает SSOT загрузчиком, который **отказывается** от
дублирующихся ключей (`DUPLICATE-KEY`). Добавлен текстовый tamper-кейс — parsed-документ
дубликат представить не может, поэтому кейс идёт через `validate()` на реальных байтах.

Заодно гейт поймал два следствия правок: новый скрипт-мутатор не был объявлен, и проекции
разошлись после исправления evidence. Оба закрыты; множество мутаторов остаётся замкнутым.

Отдельно: я на секунду принял `exit=0` после `| tail` за ложный зелёный код возврата.
Это была ошибка измерения — `$?` брался у `tail`. `main()` возвращает 1 корректно;
все финальные прогоны сняты без пайпа.

## .77 выведен из эксплуатации · RM-ENV-001 закрыт · этап S начат

### OD-016 — решение владельца

Владелец вывел `192.168.110.77` из эксплуатации 2026-08-26. Записано как `OD-016`
(`approved`), уточняет OD-007 в части `.77`; клауза про активный baseline `.81` в силе.

Правило гейта расширено так, чтобы **отличать решение от наблюдения**: диспозиции
`decommissioned` и `scheduled-upgrade` допустимы только при ссылке на **одобренное**
решение владельца в SSOT и при указанной дате. Без ссылки, с висячей ссылкой, с
неодобренным решением или без даты — красный. Четыре tamper-кейса на это.

`RM-ENV-001` → `done`, `owner_gate.granted: true`. Помечены артефакты выведенного хоста:
`docs/runbook/local-preview.md` получил баннер `SUPERSEDED`,
`infra/compose/docker-compose.preview.yml` — шапку с причиной.

### RM-STAB-001 — единый контракт `BEHAVIORAL_APP_DB_URL`

Закрывает мою находку `BEHAVIORAL-ENV-CONTRACT-001`. `tests/behavioral/dsn.py` — одно
место, читающее переменную; **12 потребителей** переведены, прямых чтений вне helper нет.
Значение задаётся любой из двух форм, потребитель берёт нужную (`sqlalchemy_dsn()` /
`raw_dsn()`); синхронные драйверы отвергаются с объяснением.

Доказано на одноразовой PostgreSQL (схема `036`, роль `NOBYPASSRLS`, 37 таблиц FORCE RLS,
контейнер удалён): обе формы дают `9 passed` на targeted-команде и `29 passed` на
SQLAlchemy+raw вместе. **Tamper-цикл красный в обе стороны** — теми же двумя ошибками,
которые я предъявлял в аудите.

**Две ошибки полного набора — не мои.** `test_edge003_pop_ingestion` падает
`468 passed, 2 failed`; проверено, что те же два теста падают на оригинале файла из git
и вовсе без переменной, а в CI на `75c4135` оба `PASSED` (470 passed). Причина —
различие стенда: CI поднимает PostgreSQL под owner-ролью `retail_media_owner`, мой
одноразовый стенд — под `retail_media`. Дальше не копал: вне объёма задачи.

Исправлен источник ловушки: раздел «Environment» в `PROJECT_STATE.md` больше не отправляет
оператора в дефект.

Статус `verification`, не `done`: правка идёт в CI впервые, а я в этой сессии дважды
заявлял зелёное по проверке только на своей машине. Переведу после behavioral-job.

### RM-STAB-001 закрыт прогоном CI

Прогон `33006795900` на `00d75a6` — success, 0 провалов. Behavioral-job: **470 passed,
12 skipped** — ровно столько же, сколько до правки. Ни один тест не потерян и не ослаблен.

При записи evidence правило `EVIDENCE-KIND` отвергло `done`: приёмка заявляет
`verified_by: command`, а я записал прогоны как `behavioral`. Ослаблять правило под свои
данные не стал — добавил ровно ту команду, которую называет приёмка, и которую
действительно прогнал дважды, по разу на каждую форму DSN.

## RM-STAB-002 — strict RLS context; вскрыт продовый дефект device-онбординга

Закрывает мою находку `BEHAVIORAL-ADMIN-MASK-001`. Элевация до admin привязана к фазе
прогона: `setup`/`teardown` элевированы, тело теста — strict, как в проде.

**Замер до реализации:** наивный strict ломает 181 из 470 тестов, из них
148 setup + 13 teardown + 20 тел. То есть маска подпирала построение состояния, а не
проверки — отсюда фазовое решение. После него: 0 setup/teardown-поломок, 30 тел.

### Эти 30 вскрыли продовый дефект

Все падали на `device_onboarding_codes`. Проверено по цепочке:
`POST /device/onboard` и `POST /identity/device-codes` **не несут** `set_rls_context`
(112 из 124 эндпоинтов несут); продовый `get_db` не ставит ни admin-флага, ни scope;
политики таблицы требуют одного из двух, таблица под `FORCE RLS`.

**Прямое доказательство из базы:** владелец с admin видит активный код **1**,
роль приложения **без контекста — 0**, она же с admin — **1**.

Значит в проде каждое устройство получает `403 INVALID_CODE`, self-onboarding не
работает вовсе. Латентно только потому, что пилот не развёрнут. Набор скрывал это всю
свою жизнь, потому что каждый запрос открывался администратором.

Остальные 11 эндпоинтов без контекста законны: pre-auth, device-auth, публичные и
глобальные не-RLS таблицы.

### Решение владельца по объёму

`RM-STAB-002` закрывается в объёме harness; починка device-маршрутов вынесена в новую
задачу **`RM-TECH-210`** (этап BT, `owner_gate: device_contract`). Очередь — 43 задачи.
Owner gate не формальный: онбординг неаутентифицирован, scope нельзя вывести из
пользователя — нужно решение о модели, а не правка кода.

### Подотчётный allowlist

Две записи, каждая с идентификатором дефекта, доказательством и фразой «снимается
починкой эндпоинта, а не правкой теста». Подотчётность проверяется тестом: короткая
причина, отсутствие идентификатора или больше пяти записей — красный.
`skip`/`xfail` не использованы: контракт их запрещает, и они спрятали бы ровно то,
что задача вскрывает.

### Прогоны

| Прогон | Результат |
|---|---|
| baseline | 470 passed |
| наивный strict | 20 failed, 303 passed, 161 error |
| фазовый | 440 passed, 30 failed, 0 errors |
| фазовый + allowlist | **470 passed, 12 skipped** |

Tamper красный по трём осям (маска возвращена; хук фазы убран; запись allowlist без
дефекта), после восстановления зелено.

### Две ошибки процесса

Первый замер оборвался — сделал `git stash` по `conftest.py`, пока по нему шёл pytest.
Второй был невалиден: БД осталась загрязнённой, внешние ключи блокировали очистку и
дали 293 каскадные ошибки. Оба выброшены; стенд пересоздаётся скриптом перед замером.
