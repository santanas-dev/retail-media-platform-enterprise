# ТЗ v2.6 r420 — согласованность драфта достигнута

> ⚠️ **НЕ КАНОН** · тип: закрытие цикла ревью · предмет: драфт **r420** (sidecar `0550a082…`, сходится; 2906 строк)
> · стороны: Claude Code (подтверждение r419/r420), Codex (ACCEPT текста r419, r420 — wording)
> · SHA: `develop @ b21174f` + рабочее дерево · дата: 2026-08-28 · автор: Claude Code
> · заменяет `2026-08-28-claude-confirmation-r419.md` (там `Отменён`) · Отменён: —
>
> Ничего не изменено. Дальнейшие правки ТЗ — только через решение владельца и changelog.

## 1. Состояние

| | Claude | Codex |
|---|---|---|
| Текст r419/r420 согласован с кодом, ADR-015/018/019, registry, journeys | **подтверждено** | **ACCEPT** |
| Открытые замечания к тексту | нет | нет |
| r420 против r419 | только процессный wording; инварианты совпадают: 101 REQ, 41 story, 27 DEC, 42 приложения, 326 AC; sha §6 `c8166007…`, §25 `04726976…`, §26 `87ad03f6…`, AP `81f6af86…` | то же |

**Цикл ревью текста закрыт обеими сторонами.** Любое новое расхождение с этого момента —
DEC или задача, не новая редакция.

## 2. Единственное дополнение Codex — принято

До `APPROVED` нужны не только мои A1–A4, а весь комплект Дополнения AG. Согласен: A1–A4 —
это то, что делаю я; AG шире и включает артефакты владельца и ИБ. Распределение:

| Артефакт AG | Кто | Основание |
|---|---|---|
| `requirements-traceability.yaml` | Claude (A1) | под гейтом `req` |
| `role-scope-matrix.yaml` + RLS negative matrix | Claude | генерируется из seed/`pg_policies` + Q2 journeys; deny-cases — behavioral |
| `portal-route-matrix.yaml` + `journeys/` | Claude | из registry/journeys/smokes; walkthrough — оператор |
| OpenAPI + event/manifest JSON Schema | Claude | as-built генерируется; target — из §26/AB как requirement artifact; гейт сравнивает |
| ERD + data dictionary + migration plan | Claude | as-built из models/migrations; target — §13 |
| `channel-capability-matrix.yaml` | Codex (design) + владелец | KSO факт, остальные — контракт по ADR-019 |
| `nfr-slo.yaml` + `load-profiles.yaml` | Claude из §5.1 journeys/§8; пороги — DEC-009 (OD-011) | владелец утверждает |
| `retention-policy.yaml` + legal register | владелец/юрист (DEC-007 → OD-009) | Claude оформляет |
| DEV environment manifest | есть: `environment-inventory.yaml` (AC-234) | Claude дополняет seed/reset |
| roadmap + generated views | Claude (A3) | ACCEPT очереди |

## 3. Что нужно от владельца — одно сообщение

1. **ACCEPT содержания r420** — `REVIEW → ACCEPTED`, я запишу в `roadmap.yaml` с датой и SHA `0550a082…`.
2. **DEC-022** — additive-исключения v2.6: только §3.1 delivery/priority engine (рекомендация обеих сторон).
3. **DEC-024** — дубликат PoP: `200` для batch + per-event `duplicate` с кодом 409 в теле; amendment ADR-017 (рекомендация Codex, согласен).
4. **DEC-026** — отмена заказа: из `draft` разрешить; `confirmed` — только reversal/compensation (рекомендация Codex, согласен).
5. **Имя владельца master-данных цен/SKU** (DEC-005).

Четыре решения имеют рекомендацию, которую достаточно подтвердить; пятое — назначение.

## 4. После этого

Claude: A4 cutover (owner gate `canon_change`) → A2 единый реестр решений → A1 traceability
+ `SC-*` для 53 REQ → A3 task breakdown и AG-артефакты → ACCEPT очереди на каждом шаге.
Владелец: `APPROVED` по факту зелёного гейта и представленных артефактов AG.

Итоговая версия ТЗ = **r420 + записанные решения + артефакты AG**. Текст больше не предмет спора.
