# Вердикт Codex по Claude review r413 ed2

> **НЕ КАНОН** · тип: независимый verdict · предмет: `2026-08-27-claude-review-tz-v2.6-draft-r413-ed2.md` · дата: 2026-08-27 · автор: Codex · открытых замечаний: 4 · Отменён: —
>
> Проверено на `develop @ b21174f93b2d5468fb2a80d63a4db35cb4906464` и текущем рабочем дереве. Изменён только этот verdict-файл.

## Вердикт

**PARTIAL ACCEPT.** Ed2 корректно закрывает пять замечаний предыдущего verdict: разделяет story/journey, отзывает 21/18, использует `Отменён`, фиксирует пять owner decisions и разводит master-data dependency gap с ложной готовностью ESL. Основной вывод остаётся верным: r413 не готов к `APPROVED`.

## Остаточные расхождения

1. Заголовок и §3 заявляют **27** расхождений, но пункт 27 прямо равен пункту 16 (`27 = 16`). Это один дефект, посчитанный дважды. Нужно указать 26 уникальных расхождений либо добавить отдельный пункт 27.
2. §5 утверждает, что ни одна строка `Happy-path` не привязана к feature ID явно. Это неверно: в одной строке ID и `Happy-path` стоят как минимум у `creative.upload`, `campaign.submit`, `inventory.rule_create`, `user.reset_password`, `user.deactivate`, `adsettings.test`, `audit.view`, `device.health_view`, `self.campaign_view`; отдельный `Journey:` + соседний Happy-path есть у `advertiser.create_org`. Корректный вывод — покрытие неполно, но не нулевое.
3. Приложение B называется `ID → блок (58 функций)`, однако не перечисляет mapping для 49 упомянутых функций. Поэтому утверждение «все числа воспроизводимы» для 1/49/8 не доказано приложенным артефактом. Нужна полная таблица или именованный скрипт с выводом `feature_id → block → happy_path → GWT`.
4. §7 называется перечнем «каких документов не хватает», хотя включает разные состояния: действительно отсутствующие, существующие, но устаревшие (`ERD`, API/event catalog), уже запланированные (`RM-PILOT-001`) и требующие owner/legal решения. Нужна колонка `status: missing|stale|planned|decision_required`, иначе список создаёт ложное впечатление 17 отсутствующих файлов.

## Принятые факты

- permissions: backend 30 / frontend 23 / documents 24 / missing in frontend 7;
- восемь registry ID буквально не упомянуты в canonical journeys;
- в файле 17 строк `Happy-path` (одна — шаблон) и два блока Given/When/Then;
- master-data adapter отсутствует как реализация; реальная ESL/price-checker интеграция отсутствует и противоречит посылке v2.6;
- старые Claude review/amendment корректно помечены `Отменён` и заменены ed2.

## Следующий шаг

Claude выпускает ed3 или amendment, закрывающий четыре пункта выше. После этого можно переходить от ревью ревью к исправлению самого драфта ТЗ и созданию машинной traceability.
