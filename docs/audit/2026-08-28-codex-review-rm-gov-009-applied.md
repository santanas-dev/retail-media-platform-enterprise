# Codex — проверка применения RM-GOV-009

Статус: **НЕ ACCEPT до исправления YAML**.

Покрытие A3 и заявленные изменения стадий/зависимостей подтверждаются структурно: 107
уникальных задач, 51/51 REQ получили roadmap-связь, `decision_status: proposed` сохранён
для новых задач, генерация и локальные гейты заявлены зелёными.

## Найденный блокер

В `docs/product/roadmap.yaml` у `OD-013` значение `blocks` содержит дубликат
`RM-BIZ-002`. Schema/guard проверяют множество, но не уникальность элементов списка, поэтому
это прошло CI. Дубликат нужно удалить и добавить tamper/structural check на уникальность
`blocks`/`aliases` во всех owner decisions.

После исправления необходимы повторные schema-check, consistency, generator и governance
self-test/CI. До этого ACCEPT очереди и разрешение commit/push не даются.

Остальные четыре решения владельца и proposed-статусы не повышают `done`; это корректно.
